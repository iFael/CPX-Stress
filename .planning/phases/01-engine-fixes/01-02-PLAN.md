---
phase: 01-engine-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - electron/engine/stress-engine.ts
  - electron/engine/stress-worker.ts
autonomous: true
requirements:
  - ENGINE-02
  - ENGINE-03

must_haves:
  truths:
    - "makeRequest segue até 5 redirects 3xx, retornando a URL final após a última resposta não-redirect"
    - "Cookies Set-Cookie são capturados em cada hop intermediário (o cookieJar recebe cookies do 302 antes de seguir para a próxima URL)"
    - "O corpo HTML da resposta final é coletado para extraction — {{CTRL}} é extraído do destino final, não do corpo vazio do 302"
    - "Cada VU executa a cadeia authOps exatamente uma vez ao iniciar, sem re-autenticar em todo ciclo"
    - "Re-autenticação ocorre somente quando finalUrl.pathname === loginPathname (sessão expirada detectada)"
    - "cookieJar.clear() e extractedVars.clear() são chamados antes de re-autenticar para garantir estado limpo"
    - "Em testes sem operações de módulo (moduleOps vazio), o comportamento original é preservado — authOps continua em loop"
  artifacts:
    - path: "electron/engine/stress-engine.ts"
      provides: "makeSingleRequest + makeRequest com redirect loop + executeOp retornando URL + spawnVU reestruturado"
      contains: "makeSingleRequest"
    - path: "electron/engine/stress-worker.ts"
      provides: "makeSingleRequest + makeRequest com redirect loop + executeOp retornando URL + runVU reestruturado"
      contains: "makeSingleRequest"
  key_links:
    - from: "executeOp (engine spawnVU)"
      to: "makeRequest → finalUrl"
      via: "return result.finalUrl dentro de executeOp"
      pattern: "return result\\.finalUrl"
    - from: "executeOp (worker runVU)"
      to: "makeRequest → finalUrl"
      via: "return result.finalUrl dentro de executeOp do worker"
      pattern: "return result\\.finalUrl"
    - from: "spawnVU (engine)"
      to: "cookieJar.clear() + re-auth"
      via: "sessionExpired check com loginPathname"
      pattern: "cookieJar\\.clear\\(\\)"
    - from: "runVU (worker)"
      to: "cookieJar.clear() + re-auth"
      via: "sessionExpired check com loginPathname"
      pattern: "cookieJar\\.clear\\(\\)"
---

<objective>
Corrige dois bugs interdependentes no motor de stress que afetam ambos os arquivos de engine:

1. ENGINE-02: makeRequest não segue redirects 3xx. No fluxo ASP Classic do MisterT, o login POST retorna 302 com o CTRL no Location header — sem redirect following, o corpo final nunca é coletado e {{CTRL}} nunca é extraído. Fix: separar em makeSingleRequest (lógica atual + locationHeader) + makeRequest (wrapper com loop de até 5 hops, retorna finalUrl).

2. ENGINE-03: O loop de VU re-autentica a cada iteração (chama authOps no início de todo ciclo while). Com 100 VUs em 600s, isso gera centenas de logins por minuto — "auth storm". Fix: executar authOps UMA VEZ ao iniciar o VU, loopando apenas moduleOps. Re-autenticar somente quando finalUrl.pathname === pathname do login (sessão expirada). ENGINE-03 depende do finalUrl retornado pelo ENGINE-02.

Ambas as mudanças se aplicam a stress-engine.ts (modo single-threaded) e stress-worker.ts (modo worker threads, >256 VUs). O modo single-threaded (50-100 VUs) é o caminho crítico do MisterT.

Purpose: Permitir extração correta de {{CTRL}} e eliminar auth storm.
Output: stress-engine.ts e stress-worker.ts com redirect following e VU lifecycle correto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-fixes/01-RESEARCH.md
@.planning/phases/01-engine-fixes/01-01-SUMMARY.md

<interfaces>
<!-- Estado atual dos dois arquivos após Plan 01. Executor lê estes contratos antes de editar. -->

De electron/engine/stress-engine.ts (estado após Plan 01):

// makeRequest atual — será renomeada para makeSingleRequest e receberá locationHeader (linha ~1206)
private makeRequest(
  opts: {
    url: URL;
    isHttps: boolean;
    agent: http.Agent | https.Agent;
    signal: AbortSignal;
    method: string;
    headers?: Record<string, string>;
    body?: string;
    cookieJar: CookieJar;
    captureSession: boolean;
    collectBody: boolean;
  },
  captureSample: boolean = false,
): Promise<{
  statusCode: number;
  bytes: number;
  sample?: ResponseSample;
  bodyText?: string;
  // locationHeader?: string  ← ENGINE-02 adiciona
}>

// executeOp atual — retorna Promise<void>, passará a retornar Promise<URL | undefined> (linha ~1075)
const executeOp = async (op: TestOperation): Promise<void> => {
  // ...
  const result = await this.makeRequest({ ... }, captureSample);
  // result.finalUrl não existe ainda — ENGINE-02 adiciona
  // return result.finalUrl;  ← ENGINE-03 precisa disso
}

// Loop do VU atual com o bug (linhas ~1158-1174)
while (Date.now() < opts.endTime && !opts.signal.aborted) {
  for (const op of authOps) { await executeOp(op); }  // ← re-auth todo ciclo (BUG)
  if (moduleOps.length === 0) { continue; }
  const randomModule = moduleOps[Math.floor(Math.random() * moduleOps.length)];
  await executeOp(randomModule);
}

// CookieJar.clear() existe (confirmado na pesquisa — cookie-jar.ts linha ~81)
// Acessível via cookieJar.clear()

De electron/engine/stress-worker.ts (estado atual):

// makeRequest worker — será renomeada para makeSingleRequest (linha ~126)
function makeRequest(
  opts: {
    url: URL;
    isHttps: boolean;
    method: string;
    headers?: Record<string, string>;
    body?: string;
    cookieJar: CookieJar;
    captureSession: boolean;
    collectBody: boolean;
  },
  captureSample: boolean,
): Promise<{ statusCode: number; bytes: number; sample?: ResponseSample; bodyText?: string; }>

// executeOp no worker — retorna Promise<void> (linha ~330)
const executeOp = async (op: WorkerOperation): Promise<void> => { ... }

// Loop do worker com o mesmo bug (linhas ~412-427)
while (Date.now() < config.endTime && !signal.aborted) {
  for (const op of authOps) { await executeOp(op); }  // ← re-auth todo ciclo (BUG)
  if (moduleOps.length === 0) { continue; }
  const randomModule = moduleOps[Math.floor(Math.random() * moduleOps.length)];
  await executeOp(randomModule);
}

// agents module-level disponíveis para seleção no redirect loop:
const agents = { http: http.Agent, https: https.Agent }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ENGINE-02 — Redirect following em makeRequest (engine + worker)</name>

  <read_first>
    1. electron/engine/stress-engine.ts — ler o arquivo completo. Localizar:
       - O método makeRequest() (linha ~1206) — entender assinatura, opts, Promise resolve
       - O bloco `res.on('end', ...)` — onde locationHeader será capturado
       - O método executeOp() dentro de spawnVU (linha ~1075) — onde finalUrl será retornado
    2. electron/engine/stress-worker.ts — ler o arquivo completo. Localizar:
       - A função makeRequest() (linha ~126) — equivalente no worker
       - O bloco `res.on('end', ...)` — mesma mudança
       - O executeOp dentro de runVU (linha ~330) — mesma mudança
  </read_first>

  <files>electron/engine/stress-engine.ts, electron/engine/stress-worker.ts</files>

  <action>
    =====================================================================
    STRESS-ENGINE.TS — Mudanças ENGINE-02
    =====================================================================

    PASSO A — Renomear makeRequest para makeSingleRequest e adicionar locationHeader:

    1. Renomear o método `private makeRequest(...)` para `private makeSingleRequest(...)`.
       A assinatura dos parâmetros (opts + captureSample) permanece idêntica.

    2. Atualizar o tipo de retorno da Promise para incluir locationHeader:
       ```typescript
       ): Promise<{
         statusCode: number;
         bytes: number;
         sample?: ResponseSample;
         bodyText?: string;
         locationHeader?: string;  // ← LINHA NOVA
       }>
       ```

    3. Dentro do callback `res.on('end', () => { ... })`, ANTES da linha `resolve(...)`,
       capturar o header Location:
       ```typescript
       const locationHeader = typeof res.headers['location'] === 'string'
         ? res.headers['location']
         : undefined;
       ```
       Então passar locationHeader no resolve:
       ```typescript
       resolve({ statusCode: res.statusCode ?? 0, bytes, sample, bodyText, locationHeader });
       ```

    PASSO B — Criar novo método makeRequest com loop de redirects:

    Adicionar após makeSingleRequest o seguinte método privado assíncrono:

    ```typescript
    private async makeRequest(
      opts: {
        url: URL;
        isHttps: boolean;
        agent: http.Agent | https.Agent;
        signal: AbortSignal;
        method: string;
        headers?: Record<string, string>;
        body?: string;
        cookieJar: CookieJar;
        captureSession: boolean;
        collectBody: boolean;
      },
      captureSample: boolean = false,
    ): Promise<{
      statusCode: number;
      bytes: number;
      sample?: ResponseSample;
      bodyText?: string;
      finalUrl: URL;
    }> {
      const MAX_REDIRECT_HOPS = 5;
      let currentUrl = opts.url;
      let currentIsHttps = opts.isHttps;
      let currentMethod = opts.method;
      let currentBody = opts.body;

      // Usar agente correto por protocolo
      const selectAgent = (isHttps: boolean): http.Agent | https.Agent =>
        isHttps ? this.activeAgents.https! : this.activeAgents.http!;

      let lastResult: Awaited<ReturnType<typeof this.makeSingleRequest>> | null = null;

      for (let hop = 0; hop <= MAX_REDIRECT_HOPS; hop++) {
        const isLastHop = hop === MAX_REDIRECT_HOPS;
        lastResult = await this.makeSingleRequest(
          {
            ...opts,
            url: currentUrl,
            isHttps: currentIsHttps,
            method: currentMethod,
            body: currentBody,
            agent: selectAgent(currentIsHttps),
            // Coletar body apenas no hop final para extraction
            collectBody: opts.collectBody && (isLastHop || !this.isRedirectStatus(lastResult?.statusCode ?? 0)),
          },
          // captureSample apenas no hop final
          captureSample && (isLastHop || !this.isRedirectStatus(lastResult?.statusCode ?? 0)),
        );

        const { statusCode, locationHeader } = lastResult;
        const isRedirect = this.isRedirectStatus(statusCode);

        if (!isRedirect || !locationHeader || isLastHop) {
          break;
        }

        // Resolver URL relativa ou absoluta contra a URL atual
        const redirectUrl = new URL(locationHeader, currentUrl.toString());

        // RFC 7231: 302 e 303 sempre mudam para GET e descartam o body
        if (statusCode === 302 || statusCode === 303) {
          currentMethod = 'GET';
          currentBody = undefined;
        }
        // 307 e 308: preservar método e body originais

        currentUrl = redirectUrl;
        currentIsHttps = redirectUrl.protocol === 'https:';
      }

      return {
        statusCode: lastResult!.statusCode,
        bytes: lastResult!.bytes,
        sample: lastResult!.sample,
        bodyText: lastResult!.bodyText,
        finalUrl: currentUrl,
      };
    }
    ```

    PASSO C — Adicionar método auxiliar isRedirectStatus:

    Adicionar como método privado (após makeRequest):
    ```typescript
    private isRedirectStatus(statusCode: number): boolean {
      return statusCode >= 300 && statusCode <= 308 && statusCode !== 304;
    }
    ```

    PASSO D — Atualizar executeOp para capturar e retornar finalUrl:

    Dentro de spawnVU, a função interna `executeOp` tem assinatura `async (op): Promise<void>`.
    Mudar para `Promise<URL | undefined>`.

    No bloco try dentro de executeOp, após `const result = await this.makeRequest(...)`:
    - Localizar a linha `opts.onResponse(latency, result.statusCode, result.bytes, ...)` e mantê-la.
    - Após o bloco de extraction (if hasExtract...) e ANTES do `opts.onResponse(...)`,
      capturar `const latency = performance.now() - start;`
      (isso já existe — não duplicar, apenas verificar a ordem)
    - Adicionar `return result.finalUrl;` como última linha do bloco try.
    - No bloco catch, garantir que retorna implicitamente undefined (sem alteração necessária).

    O tipo de retorno de executeOp passa a ser `Promise<URL | undefined>`.

    =====================================================================
    STRESS-WORKER.TS — Mudanças ENGINE-02
    =====================================================================

    PASSO E — Renomear makeRequest para makeSingleRequest no worker:

    1. Renomear a função `function makeRequest(...)` para `function makeSingleRequest(...)`.
       Mesmos parâmetros, mesmo corpo.

    2. Adicionar `locationHeader?: string` ao tipo de retorno da Promise:
       ```typescript
       ): Promise<{ statusCode: number; bytes: number; sample?: ResponseSample; bodyText?: string; locationHeader?: string; }>
       ```

    3. No `res.on('end', ...)`, antes do `resolve(...)`, capturar:
       ```typescript
       const locationHeader = typeof res.headers['location'] === 'string'
         ? res.headers['location']
         : undefined;
       resolve({ statusCode: res.statusCode ?? 0, bytes, sample, bodyText, locationHeader });
       ```

    PASSO F — Criar nova função makeRequest no worker com redirect loop:

    Adicionar após makeSingleRequest no worker (função module-level, não class method):

    ```typescript
    async function makeRequest(
      opts: {
        url: URL;
        isHttps: boolean;
        method: string;
        headers?: Record<string, string>;
        body?: string;
        cookieJar: CookieJar;
        captureSession: boolean;
        collectBody: boolean;
      },
      captureSample: boolean,
    ): Promise<{
      statusCode: number;
      bytes: number;
      sample?: ResponseSample;
      bodyText?: string;
      finalUrl: URL;
    }> {
      const MAX_REDIRECT_HOPS = 5;
      let currentUrl = opts.url;
      let currentIsHttps = opts.isHttps;
      let currentMethod = opts.method;
      let currentBody = opts.body;

      function isRedirectStatus(statusCode: number): boolean {
        return statusCode >= 300 && statusCode <= 308 && statusCode !== 304;
      }

      let lastResult: Awaited<ReturnType<typeof makeSingleRequest>> | null = null;

      for (let hop = 0; hop <= MAX_REDIRECT_HOPS; hop++) {
        const isLastHop = hop === MAX_REDIRECT_HOPS;
        const prevStatus = lastResult?.statusCode ?? 0;
        const isFinalHop = isLastHop || !isRedirectStatus(prevStatus);

        lastResult = await makeSingleRequest(
          {
            ...opts,
            url: currentUrl,
            isHttps: currentIsHttps,
            method: currentMethod,
            body: currentMethod === 'GET' ? undefined : currentBody,
            collectBody: opts.collectBody && isFinalHop,
          },
          captureSample && isFinalHop,
        );

        const { statusCode, locationHeader } = lastResult;
        const isRedirect = isRedirectStatus(statusCode);

        if (!isRedirect || !locationHeader || isLastHop) {
          break;
        }

        const redirectUrl = new URL(locationHeader, currentUrl.toString());

        if (statusCode === 302 || statusCode === 303) {
          currentMethod = 'GET';
          currentBody = undefined;
        }

        currentUrl = redirectUrl;
        currentIsHttps = redirectUrl.protocol === 'https:';
      }

      return {
        statusCode: lastResult!.statusCode,
        bytes: lastResult!.bytes,
        sample: lastResult!.sample,
        bodyText: lastResult!.bodyText,
        finalUrl: currentUrl,
      };
    }
    ```

    PASSO G — Atualizar executeOp no worker para retornar finalUrl:

    Dentro de runVU, o executeOp interno passa de `Promise<void>` para `Promise<URL | undefined>`.
    Adicionar `return result.finalUrl;` como última linha do bloco try no executeOp do worker.

    PASSO H — Correção de branding User-Agent no worker:

    Localizar `"User-Agent": "StressFlow/1.0"` em stress-worker.ts (linha ~147).
    Alterar para `"User-Agent": "CPX-MisterT-Stress/1.0"`.
  </action>

  <acceptance_criteria>
    ENGINE (stress-engine.ts):
    1. grep -n "private makeSingleRequest" electron/engine/stress-engine.ts
       DEVE retornar 1 linha (renomeação feita)

    2. grep -n "private async makeRequest" electron/engine/stress-engine.ts
       DEVE retornar 1 linha (novo wrapper com redirect loop)

    3. grep -n "MAX_REDIRECT_HOPS" electron/engine/stress-engine.ts
       DEVE retornar 1 linha (= 5)

    4. grep -n "finalUrl" electron/engine/stress-engine.ts
       DEVE retornar pelo menos 3 linhas (declaração no retorno, retorno no loop, retorno no executeOp)

    5. grep -n "locationHeader" electron/engine/stress-engine.ts
       DEVE retornar pelo menos 3 linhas (captura no res.on end, tipo de retorno, uso no loop)

    6. grep -n "isRedirectStatus" electron/engine/stress-engine.ts
       DEVE retornar pelo menos 2 linhas (definição + uso)

    WORKER (stress-worker.ts):
    7. grep -n "function makeSingleRequest" electron/engine/stress-worker.ts
       DEVE retornar 1 linha

    8. grep -n "async function makeRequest" electron/engine/stress-worker.ts
       DEVE retornar 1 linha

    9. grep -n "MAX_REDIRECT_HOPS" electron/engine/stress-worker.ts
       DEVE retornar 1 linha (= 5)

    10. grep -n "finalUrl" electron/engine/stress-worker.ts
        DEVE retornar pelo menos 2 linhas

    11. grep -n "StressFlow/1.0" electron/engine/stress-worker.ts
        DEVE retornar 0 linhas (branding corrigido)

    12. grep -n "CPX-MisterT-Stress/1.0" electron/engine/stress-worker.ts
        DEVE retornar 1 linha
  </acceptance_criteria>

  <verify>
    <automated>cd "c:/Users/rafael.damasio/Desktop/Project/CPX-MisterT Stress" && npm run build 2>&1 | tail -30</automated>
  </verify>

  <done>
    Build TypeScript limpo. makeSingleRequest existe em ambos os arquivos. makeRequest com
    redirect loop existe em ambos. finalUrl retornado de executeOp (engine e worker).
    User-Agent atualizado no worker. Branding StressFlow/1.0 eliminado de ambos os arquivos.
  </done>
</task>

<task type="auto">
  <name>Task 2: ENGINE-03 — Reestruturação do loop de VU (engine + worker)</name>

  <read_first>
    1. electron/engine/stress-engine.ts — ler a função spawnVU() completa (linha ~1029 até ~1175).
       Focar no bloco de separação authOps/moduleOps (linhas ~1147-1156) e no while loop
       (linhas ~1158-1174). Verificar que a Task 1 já alterou executeOp para retornar
       Promise&lt;URL | undefined&gt;.
    2. electron/engine/stress-worker.ts — ler a função runVU() completa (linha ~311 até ~428).
       Verificar que a Task 1 já alterou o executeOp do worker para retornar Promise&lt;URL | undefined&gt;.
    3. electron/engine/cookie-jar.ts — confirmar que o método clear() existe e aceita zero parâmetros.
  </read_first>

  <files>electron/engine/stress-engine.ts, electron/engine/stress-worker.ts</files>

  <action>
    =====================================================================
    STRESS-ENGINE.TS — Mudanças ENGINE-03
    =====================================================================

    Localizar o while loop dentro de spawnVU() que começa na linha ~1158:

    CÓDIGO ATUAL (remover):
    ```typescript
    while (Date.now() < opts.endTime && !opts.signal.aborted) {
      // Fase 1: Executar cadeia de autenticação sequencialmente
      for (const op of authOps) {
        await executeOp(op);
      }

      if (moduleOps.length === 0) {
        // Sem módulos — apenas loop de autenticação (teste single-op ou sem GET pós-auth)
        continue;
      }

      // Fase 2: Selecionar módulo aleatório do pool de operações pós-autenticação
      // Isso garante cobertura uniforme de todos os módulos, mesmo em testes curtos
      const randomModule =
        moduleOps[Math.floor(Math.random() * moduleOps.length)];
      await executeOp(randomModule);
    }
    ```

    SUBSTITUIR POR (código corrigido ENGINE-03):
    ```typescript
    // Fase de autenticação inicial — executa UMA VEZ por VU, não a cada iteração
    for (const op of authOps) {
      await executeOp(op);
    }

    // Determinar pathname da página de login para detecção de expiração de sessão
    // Quando um módulo retorna redirect para esta pathname, a sessão expirou
    const loginPathname = authOps.length > 0
      ? new URL(authOps[0].url).pathname.toLowerCase()
      : null;

    // Loop principal — apenas operações de módulo (sem re-autenticação desnecessária)
    while (Date.now() < opts.endTime && !opts.signal.aborted) {
      if (moduleOps.length === 0) {
        // Modo single-op ou auth-only: mantém comportamento original
        // (sem módulos, o loop continua executando authOps)
        for (const op of authOps) {
          await executeOp(op);
        }
        continue;
      }

      const randomModule =
        moduleOps[Math.floor(Math.random() * moduleOps.length)];
      const finalUrl = await executeOp(randomModule);

      // Detecção de expiração de sessão: módulo retornou redirect para a página de login
      const sessionExpired =
        loginPathname !== null &&
        finalUrl !== undefined &&
        finalUrl.pathname.toLowerCase() === loginPathname;

      if (sessionExpired) {
        // Limpar estado de sessão antiga antes de re-autenticar
        cookieJar.clear();
        extractedVars.clear();
        // Re-autenticar: executar a cadeia de auth sequencialmente
        for (const op of authOps) {
          await executeOp(op);
        }
      }
    }
    ```

    =====================================================================
    STRESS-WORKER.TS — Mudanças ENGINE-03
    =====================================================================

    Localizar o while loop dentro de runVU() que começa na linha ~412:

    CÓDIGO ATUAL (remover):
    ```typescript
    while (Date.now() < config.endTime && !signal.aborted) {
      // Fase 1: Executar cadeia de autenticação sequencialmente
      for (const op of authOps) {
        await executeOp(op);
      }

      if (moduleOps.length === 0) {
        // Sem módulos — apenas loop de autenticação
        continue;
      }

      // Fase 2: Selecionar módulo aleatório do pool de operações pós-autenticação
      const randomModule =
        moduleOps[Math.floor(Math.random() * moduleOps.length)];
      await executeOp(randomModule);
    }
    ```

    SUBSTITUIR POR (código corrigido ENGINE-03 — idêntico ao engine, adaptado para worker):
    ```typescript
    // Fase de autenticação inicial — executa UMA VEZ por VU
    for (const op of authOps) {
      await executeOp(op);
    }

    // Determinar pathname da página de login para detecção de expiração de sessão
    const loginPathname = authOps.length > 0
      ? new URL(authOps[0].url).pathname.toLowerCase()
      : null;

    // Loop principal — apenas operações de módulo
    while (Date.now() < config.endTime && !signal.aborted) {
      if (moduleOps.length === 0) {
        // Modo single-op ou auth-only: mantém comportamento original
        for (const op of authOps) {
          await executeOp(op);
        }
        continue;
      }

      const randomModule =
        moduleOps[Math.floor(Math.random() * moduleOps.length)];
      const finalUrl = await executeOp(randomModule);

      // Detecção de expiração de sessão
      const sessionExpired =
        loginPathname !== null &&
        finalUrl !== undefined &&
        finalUrl.pathname.toLowerCase() === loginPathname;

      if (sessionExpired) {
        cookieJar.clear();
        extractedVars.clear();
        for (const op of authOps) {
          await executeOp(op);
        }
      }
    }
    ```

    ATENÇÃO — Verificar comentários existentes:
    Os comentários acima dos blocos authOps/moduleOps (linhas ~1142-1146 no engine e
    ~396-400 no worker) descrevem a lógica de separação authOps/moduleOps. Esses comentários
    podem ser preservados ou atualizados para refletir que authOps agora roda apenas uma vez.
    Não é bloqueante — priorizar a corretude do código.
  </action>

  <acceptance_criteria>
    ENGINE (stress-engine.ts):
    1. grep -n "loginPathname" electron/engine/stress-engine.ts
       DEVE retornar exatamente 2 linhas (declaração + uso no sessionExpired check)

    2. grep -n "sessionExpired" electron/engine/stress-engine.ts
       DEVE retornar exatamente 2 linhas (declaração const + if check)

    3. grep -n "cookieJar.clear()" electron/engine/stress-engine.ts
       DEVE retornar exatamente 1 linha (dentro do bloco if (sessionExpired))

    4. grep -n "extractedVars.clear()" electron/engine/stress-engine.ts
       DEVE retornar exatamente 1 linha (logo após cookieJar.clear())

    5. Verificar ausência do padrão de re-auth no while original:
       grep -A3 "while (Date.now" electron/engine/stress-engine.ts
       A linha imediatamente após o while NÃO deve ser "for (const op of authOps)"
       (authOps agora roda ANTES do while, não dentro)

    WORKER (stress-worker.ts):
    6. grep -n "loginPathname" electron/engine/stress-worker.ts
       DEVE retornar exatamente 2 linhas

    7. grep -n "sessionExpired" electron/engine/stress-worker.ts
       DEVE retornar exatamente 2 linhas

    8. grep -n "cookieJar.clear()" electron/engine/stress-worker.ts
       DEVE retornar exatamente 1 linha

    9. grep -n "extractedVars.clear()" electron/engine/stress-worker.ts
       DEVE retornar exatamente 1 linha

    BUILD:
    10. npm run build passa sem erros TypeScript
        (o TypeScript valida que executeOp retorna URL | undefined e que finalUrl é usado corretamente)
  </acceptance_criteria>

  <verify>
    <automated>cd "c:/Users/rafael.damasio/Desktop/Project/CPX-MisterT Stress" && npm run build 2>&1 | tail -30</automated>
  </verify>

  <done>
    Build TypeScript limpo. loginPathname declarado em ambos os arquivos. sessionExpired
    verificado em ambos. cookieJar.clear() + extractedVars.clear() presentes antes de re-auth
    em ambos. O while loop em engine e worker NÃO começa mais com `for (const op of authOps)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| makeRequest → redirect chain | Cada hop segue Location header fornecido pelo servidor; redirects para hosts diferentes são resolvidos com new URL() |
| executorOp → finalUrl | finalUrl retornado pelo makeRequest é usado apenas para comparação de pathname; não é re-validado contra SSRF |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | makeRequest redirect loop | mitigate | MAX_REDIRECT_HOPS = 5 hard limit; loop termina após 5 hops independentemente do Location header; previne redirect bomb |
| T-02-02 | Information Disclosure | cross-scheme redirect (HTTPS → HTTP) | accept | Ferramenta testa ERP interno via HTTP; não há dados sensíveis que necessitem proteção TLS no caminho de redirect; o agent é selecionado corretamente por protocolo em cada hop |
| T-02-03 | Spoofing | sessionExpired pathname comparison | accept | Comparação usa pathname.toLowerCase() — não é uma decisão de segurança; apenas detecta redirect do servidor para re-autenticar; um servidor malicioso não está no modelo de ameaça desta ferramenta interna |
</threat_model>

<verification>
Após execução das duas tarefas:

1. `npm run build` passa sem erros TypeScript
2. `grep -n "makeSingleRequest" electron/engine/stress-engine.ts` → 1 resultado
3. `grep -n "makeSingleRequest" electron/engine/stress-worker.ts` → 1 resultado
4. `grep -n "finalUrl" electron/engine/stress-engine.ts` → 3+ resultados
5. `grep -n "finalUrl" electron/engine/stress-worker.ts` → 2+ resultados
6. `grep -n "loginPathname" electron/engine/stress-engine.ts` → 2 resultados
7. `grep -n "loginPathname" electron/engine/stress-worker.ts` → 2 resultados
8. `grep -n "cookieJar.clear()" electron/engine/stress-engine.ts` → 1 resultado
9. `grep -n "cookieJar.clear()" electron/engine/stress-worker.ts` → 1 resultado
10. `grep -rn "StressFlow/1.0" electron/` → 0 resultados (branding eliminado de ambos)
</verification>

<success_criteria>
- ENGINE-02: makeSingleRequest existe (renomeado) em ambos os arquivos com locationHeader no resolve
- ENGINE-02: makeRequest (novo) existe em ambos os arquivos com loop MAX_REDIRECT_HOPS = 5
- ENGINE-02: executeOp retorna Promise&lt;URL | undefined&gt; em ambos os arquivos com return result.finalUrl
- ENGINE-03: Autenticação inicial (authOps) ocorre ANTES do while loop, não dentro dele
- ENGINE-03: Loop while contém apenas operações de módulo (moduleOps)
- ENGINE-03: Detecção de sessão expirada via loginPathname presente em ambos
- ENGINE-03: cookieJar.clear() + extractedVars.clear() chamados antes de re-auth em ambos
- Build TypeScript limpo sem erros de tipo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-engine-fixes/01-02-SUMMARY.md` com:
- Arquivos modificados e mudanças aplicadas
- Resultado do npm run build
- Saída dos greps de verificação (todos os 10 checks acima)
- Quaisquer observações sobre decisões tomadas durante a implementação
- Nota sobre assunção A2 (loginPathname = authOps[0].url pathname): confirmar se o
  MisterT real usa o mesmo pathname na expiração de sessão ou se requer ajuste
</output>
