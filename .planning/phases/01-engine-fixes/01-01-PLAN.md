---
phase: 01-engine-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/engine/stress-engine.ts
autonomous: true
requirements:
  - ENGINE-01
  - ENGINE-04

must_haves:
  truths:
    - "Quando STRESSFLOW_ALLOW_INTERNAL=true está no .env, validateTargetHost() retorna sem erro para hosts 10.x.x.x e 192.168.x.x"
    - "Sem STRESSFLOW_ALLOW_INTERNAL (ou com valor diferente de 'true'), hosts privados continuam bloqueados com a mesma mensagem de erro anterior"
    - "Arrays opMet.latencies nunca excedem 100.000 entradas em nenhuma operação, independente da duração ou volume do teste"
    - "A distribuição estatística dos dados de latência por operação é preservada via reservoir sampling (mesmo algoritmo do latencyReservoir global)"
  artifacts:
    - path: "electron/engine/stress-engine.ts"
      provides: "Guard STRESSFLOW_ALLOW_INTERNAL + reservoir sampling por operação"
      contains: "STRESSFLOW_ALLOW_INTERNAL"
    - path: "electron/engine/stress-engine.ts"
      provides: "Campo latencySampleCount no tipo opMetrics"
      contains: "latencySampleCount"
  key_links:
    - from: "validateTargetHost()"
      to: "process.env.STRESSFLOW_ALLOW_INTERNAL"
      via: "leitura direta de process.env no main process"
      pattern: "STRESSFLOW_ALLOW_INTERNAL"
    - from: "handleResponse callback"
      to: "opMet.latencies"
      via: "reservoir sampling com opMet.latencySampleCount"
      pattern: "opMet\\.latencySampleCount\\+\\+"
---

<objective>
Corrige dois bugs independentes no motor de stress que compartilham o mesmo arquivo (stress-engine.ts):

1. ENGINE-01: validateTargetHost() bloqueia incondicionalmente IPs privados, impedindo testes contra o MisterT ERP na rede interna corporativa (10.x / 192.168.x). Fix: guard de 2 linhas que pula todas as verificações SSRF quando STRESSFLOW_ALLOW_INTERNAL=true no .env.

2. ENGINE-04: opMet.latencies.push(latency) não tem limite superior. Em testes de 600s/100VUs/10 operações, acumula ~1,2 milhão de entradas por operação (~96MB de RAM). Fix: aplicar o mesmo algoritmo de reservoir sampling já usado pelo latencyReservoir global.

Nenhuma mudança em IPC, banco de dados, renderer ou worker threads.

Purpose: Desbloquear testes contra o MisterT ERP (ENGINE-01) e prevenir crescimento anormal de memória em testes longos (ENGINE-04).
Output: stress-engine.ts modificado com guard SSRF opt-in e reservoir cap por operação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-fixes/01-RESEARCH.md

<interfaces>
<!-- Contratos existentes no arquivo — executor usa diretamente, sem explorar o codebase -->

De electron/engine/stress-engine.ts:

// Função alvo do ENGINE-01 (linha ~220)
async function validateTargetHost(hostname: string): Promise<void> {
  const normalizedHostname = hostname.startsWith("[") && hostname.endsWith("]")
    ? hostname.slice(1, -1)
    : hostname;
  // ... verificação de IP + DNS ...
}

// Constante usada pelo ENGINE-04 (linha ~643)
const RESERVOIR_MAX = 100_000;
let latencySampleCount = 0; // contador GLOBAL — ENGINE-04 adiciona contador POR OPERAÇÃO

// Tipo do opMetrics Map — precisa receber latencySampleCount (linha ~460)
private opMetrics: Map<
  string,
  {
    latencies: number[];
    // latencySampleCount: number; ← AUSENTE — ENGINE-04 adiciona este campo
    requests: number;
    errors: number;
    statusCodes: Record<string, number>;
    session: {
      authenticatedRequests: number;
      sessionFailures: number;
      sessionExpiredErrors: number;
    };
  }
> = new Map();

// Inicializador do opMetrics (linha ~620)
this.opMetrics.set(op.name, {
  latencies: [],
  // latencySampleCount: 0, ← AUSENTE — ENGINE-04 adiciona
  requests: 0,
  errors: 0,
  statusCodes: {},
  session: { authenticatedRequests: 0, sessionFailures: 0, sessionExpiredErrors: 0 },
});

// Linha com o bug do ENGINE-04 (linha ~761)
opMet.latencies.push(latency); // ← substituir por reservoir sampling

// Padrão do reservoir GLOBAL para espelhar (linhas ~740-748)
latencySampleCount++;
if (latencyReservoir.length < RESERVOIR_MAX) {
  latencyReservoir.push(latency);
} else {
  const j = Math.floor(Math.random() * latencySampleCount);
  if (j < RESERVOIR_MAX) {
    latencyReservoir[j] = latency;
  }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ENGINE-01 — Guard SSRF para rede interna corporativa</name>

  <read_first>
    electron/engine/stress-engine.ts — ler o arquivo completo antes de qualquer edição.
    Localizar a função validateTargetHost() (linha ~220) e confirmar que a primeira linha
    do corpo é `const normalizedHostname = ...`. As duas linhas do guard vão ANTES dela.
    Também localizar as duas ocorrências de "StressFlow/1.0" no mesmo arquivo (linha ~1235)
    para corrigi-las nesta mesma tarefa.
  </read_first>

  <files>electron/engine/stress-engine.ts</files>

  <action>
    MUDANÇA 1 — Guard SSRF em validateTargetHost():

    Inserir as seguintes duas linhas como PRIMEIRAS linhas do corpo da função
    validateTargetHost(), antes de `const normalizedHostname = ...`:

    ```typescript
    // Guard: permite rede interna corporativa quando opt-in explícito via .env
    const allowInternal = process.env.STRESSFLOW_ALLOW_INTERNAL === 'true';
    if (allowInternal) return;
    ```

    O restante do corpo da função permanece INTACTO — zero outras alterações.
    O comentário do JSDoc acima da função (linha ~219: "// Correção de segurança...") não é alterado.

    Decisão de design: o guard bypassa TODAS as verificações SSRF (incluindo loopback),
    não apenas RFC-1918. Isso é aceitável porque:
    (a) é opt-in explícito via .env no main process,
    (b) o renderer nunca controla este env var (contextIsolation preservado),
    (c) a ferramenta é para uso interno autorizado contra o MisterT ERP.

    MUDANÇA 2 — Correção de branding User-Agent (zero-cost fix no mesmo arquivo):

    Localizar a linha dentro do método makeRequest() que define o User-Agent:
    `"User-Agent": "StressFlow/1.0",`
    (linha ~1235, dentro de `const mergedHeaders: Record<string, string> = { ... }`)

    Alterar para:
    `"User-Agent": "CPX-MisterT-Stress/1.0",`

    Nenhuma outra mudança no arquivo nesta tarefa.
  </action>

  <acceptance_criteria>
    1. grep -n "STRESSFLOW_ALLOW_INTERNAL" electron/engine/stress-engine.ts
       DEVE retornar exatamente 1 linha dentro de validateTargetHost()
       Exemplo esperado: `  const allowInternal = process.env.STRESSFLOW_ALLOW_INTERNAL === 'true';`

    2. grep -n "if (allowInternal) return" electron/engine/stress-engine.ts
       DEVE retornar exatamente 1 linha, logo após a linha do passo 1

    3. grep -n "StressFlow/1.0" electron/engine/stress-engine.ts
       DEVE retornar 0 linhas (branding corrigido)

    4. grep -n "CPX-MisterT-Stress/1.0" electron/engine/stress-engine.ts
       DEVE retornar exatamente 1 linha dentro do makeRequest() da engine

    5. A função validateTargetHost() continua com a mesma assinatura:
       grep -n "async function validateTargetHost" electron/engine/stress-engine.ts
       DEVE retornar exatamente 1 linha
  </acceptance_criteria>

  <verify>
    <automated>cd "c:/Users/rafael.damasio/Desktop/Project/CPX-MisterT Stress" && npm run build 2>&1 | tail -30</automated>
  </verify>

  <done>
    Build TypeScript passa sem erros. grep confirma STRESSFLOW_ALLOW_INTERNAL presente em
    validateTargetHost(), User-Agent atualizado para CPX-MisterT-Stress/1.0 no engine.
    Nenhum outro comportamento alterado (função inteira preservada após o guard).
  </done>
</task>

<task type="auto">
  <name>Task 2: ENGINE-04 — Reservoir sampling para arrays de latência por operação</name>

  <read_first>
    electron/engine/stress-engine.ts — confirmar o estado atual de 3 locais antes de editar:
    1. Declaração do tipo do Map opMetrics (linha ~460-473): confirmar ausência de latencySampleCount
    2. Inicializador opMetrics.set() (linha ~620-631): confirmar ausência de latencySampleCount: 0
    3. Linha com opMet.latencies.push(latency) (linha ~761): confirmar que é um push simples sem guard
    Também confirmar que RESERVOIR_MAX = 100_000 existe (linha ~643) — será reutilizado.
  </read_first>

  <files>electron/engine/stress-engine.ts</files>

  <action>
    MUDANÇA 1 — Adicionar latencySampleCount ao tipo do Map opMetrics:

    Na declaração `private opMetrics: Map<string, { ... }>`, adicionar o campo
    `latencySampleCount: number;` logo após `latencies: number[];`:

    ```typescript
    private opMetrics: Map<
      string,
      {
        latencies: number[];
        latencySampleCount: number;  // ← LINHA NOVA — contador para reservoir sampling
        requests: number;
        errors: number;
        statusCodes: Record<string, number>;
        session: {
          authenticatedRequests: number;
          sessionFailures: number;
          sessionExpiredErrors: number;
        };
      }
    > = new Map();
    ```

    MUDANÇA 2 — Adicionar latencySampleCount: 0 no inicializador:

    Dentro do loop `for (const op of operations)`, no bloco `this.opMetrics.set(op.name, { ... })`,
    adicionar `latencySampleCount: 0,` logo após `latencies: [],`:

    ```typescript
    this.opMetrics.set(op.name, {
      latencies: [],
      latencySampleCount: 0,  // ← LINHA NOVA
      requests: 0,
      errors: 0,
      statusCodes: {},
      session: {
        authenticatedRequests: 0,
        sessionFailures: 0,
        sessionExpiredErrors: 0,
      },
    });
    ```

    MUDANÇA 3 — Substituir opMet.latencies.push(latency) por reservoir sampling:

    Localizar a linha `opMet.latencies.push(latency);` dentro do callback handleResponse
    (logo antes de `opMet.requests++;`).

    Substituir essa linha única pelo bloco de reservoir sampling a seguir,
    espelhando exatamente o padrão do latencyReservoir global (linhas ~740-748):

    ```typescript
    opMet.latencySampleCount++;
    if (opMet.latencies.length < RESERVOIR_MAX) {
      opMet.latencies.push(latency);
    } else {
      const j = Math.floor(Math.random() * opMet.latencySampleCount);
      if (j < RESERVOIR_MAX) {
        opMet.latencies[j] = latency;
      }
    }
    ```

    ATENÇÃO: A variável RESERVOIR_MAX já existe no escopo (const RESERVOIR_MAX = 100_000 na
    linha ~643). Não redeclará-la.

    ATENÇÃO: opMet.requests++ permanece na linha imediatamente seguinte — não remover.

    Nenhuma outra mudança no arquivo nesta tarefa.
  </action>

  <acceptance_criteria>
    1. grep -c "opMet\.latencySampleCount" electron/engine/stress-engine.ts
       DEVE retornar 2 (incremento opMet.latencySampleCount++ + divisor Math.random() * opMet.latencySampleCount)
       Nota: o arquivo já contém 3 ocorrências de "latencySampleCount" para o reservoir global — não confundir

    2. grep -n "opMet.latencySampleCount++" electron/engine/stress-engine.ts
       DEVE retornar exatamente 1 linha (dentro do handleResponse callback)

    3. grep -n "opMet.latencies.push(latency)" electron/engine/stress-engine.ts
       DEVE retornar 0 linhas (push simples removido; substituído pelo bloco reservoir)

    4. grep -n "latencies.push(latency)" electron/engine/stress-engine.ts
       DEVE retornar exatamente 1 linha (o push dentro do bloco `if (opMet.latencies.length < RESERVOIR_MAX)`)

    5. grep -n "opMet.latencies\[j\]" electron/engine/stress-engine.ts
       DEVE retornar exatamente 1 linha (o swap do reservoir)

    6. npm run build deve passar sem erros de tipo — o TypeScript verifica que latencySampleCount
       está declarado no tipo antes de ser incrementado como opMet.latencySampleCount++
  </acceptance_criteria>

  <verify>
    <automated>cd "c:/Users/rafael.damasio/Desktop/Project/CPX-MisterT Stress" && npm run build 2>&1 | tail -30</automated>
  </verify>

  <done>
    Build TypeScript passa sem erros. grep confirma latencySampleCount em 3 locais (tipo, init,
    incremento). Push simples eliminado. Bloco reservoir idêntico ao padrão global presente.
    O TypeScript valida o novo campo no tipo do Map (se ausente → erro de compilação).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → main process | STRESSFLOW_ALLOW_INTERNAL é lido pelo main process via loadEnvFile(); renderer não tem acesso |
| main process → stress-engine | validateTargetHost() é chamada no main process event loop; fora do alcance do renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | validateTargetHost() — bypass SSRF via STRESSFLOW_ALLOW_INTERNAL | mitigate | O env var é carregado exclusivamente em electron/main.ts via loadEnvFile(); contextIsolation=true + sandbox=true impedem o renderer de definir process.env; o bypass não é exposto via IPC em nenhuma forma |
| T-01-02 | Denial of Service | opMetrics.latencies — crescimento ilimitado de memória | mitigate | ENGINE-04 aplica reservoir cap de 100.000 entradas; o mesmo limite do latencyReservoir global; crescimento de memória passa a ser O(1) após saturação |
</threat_model>

<verification>
Após execução das duas tarefas:

1. `npm run build` passa sem erros TypeScript
2. `grep -n "STRESSFLOW_ALLOW_INTERNAL" electron/engine/stress-engine.ts` → 1 resultado em validateTargetHost
3. `grep -c "opMet\.latencySampleCount" electron/engine/stress-engine.ts` → 2 resultados (incremento + divisor)
4. `grep -n "opMet.latencies.push(latency)" electron/engine/stress-engine.ts` → 0 resultados (push simples eliminado)
5. `grep -n "CPX-MisterT-Stress/1.0" electron/engine/stress-engine.ts` → 1 resultado
</verification>

<success_criteria>
- ENGINE-01: validateTargetHost() retorna void imediatamente quando STRESSFLOW_ALLOW_INTERNAL=true, sem executar DNS resolution ou verificação de IP
- ENGINE-01: Comportamento padrão (STRESSFLOW_ALLOW_INTERNAL ausente) é idêntico ao anterior — hosts privados bloqueados com mesma mensagem
- ENGINE-04: opMet.latencySampleCount existe no tipo do Map e é inicializado com 0
- ENGINE-04: Cada push em opMet.latencies é guardado pelo reservoir (latencies.length < RESERVOIR_MAX) ou substituído via swap aleatório
- Build TypeScript limpo (sem erros de tipo introduzidos pelas mudanças)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-engine-fixes/01-01-SUMMARY.md` com:
- Arquivos modificados e mudanças aplicadas
- Resultado do npm run build
- Saída dos greps de verificação
- Quaisquer observações sobre o estado do código encontrado
</output>
