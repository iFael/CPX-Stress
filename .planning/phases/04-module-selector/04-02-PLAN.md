---
phase: 04-module-selector
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/TestConfig.tsx
autonomous: false
requirements:
  - PRESET-03
must_haves:
  truths:
    - "Ao aplicar o preset MisterT Completo, 7 checkboxes de módulos aparecem visíveis entre Configuração de Carga e Configurações Avançadas"
    - "Desmarcar um módulo individual remove apenas aquela operação de config.operations, preservando as demais"
    - "Aplicar seleção parcial resulta em config.operations com infra ops (0-2) + apenas módulos marcados"
    - "Com preset aplicado e módulos modificados, o nome do preset ativo permanece visível na toolbar"
    - "Com todos os módulos desmarcados, aviso laranja Nenhum módulo selecionado aparece abaixo do grid"
    - "Sem seleção explícita de módulos (preset recém-aplicado), todos os 7 checkboxes estão marcados"
    - "Para preset de usuário customizado sem ops MisterT, a seção de módulos não aparece"
  artifacts:
    - path: "src/components/TestConfig.tsx"
      provides: "Fieldset Módulos do Teste com 7 checkboxes renderizados condicionalmente"
      contains: "MISTERT_MODULE_METADATA"
  key_links:
    - from: "checkbox onChange"
      to: "updateModuleSelection"
      via: "handleModuleToggle filtra ops reconstruídas do template"
      pattern: "handleModuleToggle"
    - from: "isMistertPreset gate"
      to: "MISTERT_MODULE_NAMES.has(op.name)"
      via: "config.operations.some() detecta operações MisterT"
      pattern: "isMistertPreset"
---

<objective>
Implementa o seletor de módulos na UI de configuração: fieldset com 7 checkboxes, toggle
"Selecionar Todos/Limpar Seleção", contador e aviso de seleção vazia. Verificação humana
confirma o comportamento visual e interativo.

Purpose: Entrega PRESET-03 ao usuário — seleção granular de módulos MisterT via checkboxes
sem editar JSON ou criar preset do zero.
Output: Seção "Módulos do Teste" funcional em TestConfig.tsx. Comportamento verificado humanamente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-module-selector/04-CONTEXT.md
@.planning/phases/04-module-selector/04-UI-SPEC.md
@.planning/phases/04-module-selector/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos disponíveis após Plan 04-01 ser executado -->

From src/constants/test-presets.ts (novo export do Plan 04-01):
```typescript
export const MISTERT_MODULE_METADATA: readonly {
  readonly name: string;
  readonly code: string;
}[];
// 7 entradas em ordem:
// { name: "CPX-Fretes",   code: "R=89"  }
// { name: "CPX-Rastreio", code: "R=90"  }
// { name: "Estoque",      code: "R=122" }
// { name: "Ordens E/S",   code: "R=102" }
// { name: "Produção",     code: "R=84"  }
// { name: "Faturamento",  code: "R=206" }
// { name: "Financeiro",   code: "R=250" }
```

From src/stores/test-store.ts (nova ação do Plan 04-01):
```typescript
// Adicionado ao TestStore:
updateModuleSelection: (operations: TestOperation[]) => void;
// Atualiza config.operations e config.url = operations[0]?.url
// NÃO zera activePreset (diferente de updateConfig)
// Selector: useTestStore((s) => s.updateModuleSelection)
```

From src/constants/test-presets.ts (exports existentes já importados em TestConfig.tsx):
```typescript
export const MISTERT_DEFAULT_BASE_URL: string;
export const MISTERT_OPERATION_COUNT: number;
export function buildMistertOperations(baseUrl?: string): TestOperation[];
```

From src/components/WelcomeOverlay.tsx (linhas 251-283 — padrão exato de checkbox a replicar):
```tsx
<label className="flex items-center gap-2.5 mb-5 cursor-pointer group select-none">
  <div className="relative flex items-center justify-center">
    <input
      type="checkbox"
      checked={dontShowAgain}
      onChange={(e) => setDontShowAgain(e.target.checked)}
      className="peer sr-only"
    />
    {/* Caixa visual do checkbox */}
    <div className="w-[18px] h-[18px] rounded-md border border-sf-border bg-sf-surface peer-checked:bg-sf-primary peer-checked:border-sf-primary peer-focus-visible:ring-2 peer-focus-visible:ring-sf-primary/50 peer-focus-visible:ring-offset-1 peer-focus-visible:ring-offset-sf-bg transition-all duration-200">
      {dontShowAgain && (
        <svg className="w-full h-full text-white p-0.5" viewBox="0 0 12 12" fill="none" aria-hidden="true">
          <path d="M2.5 6L5 8.5L9.5 3.5" stroke="currentColor" strokeWidth="1.75" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      )}
    </div>
  </div>
  <span className="text-sm text-sf-textMuted group-hover:text-sf-textSecondary transition-colors">
    Não mostrar novamente
  </span>
</label>
```

Design tokens confirmados em tailwind.config.mjs:
- sf-primaryHover: #818cf8
- sf-textMuted: #64748b
- sf-warning: #f59e0b (sf-warning/10 = fundo do aviso)

Ponto de inserção em TestConfig.tsx:
- Após: fieldset "Configuração de carga" fecha em ~linha 365 (</fieldset>)
- Antes: div "Configurações Avançadas" abre em ~linha 367 (<div className="mb-4">)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implementar seção Módulos do Teste em TestConfig.tsx</name>
  <files>src/components/TestConfig.tsx</files>
  <read_first>
    - src/components/TestConfig.tsx — ver imports (linhas 1-27), selectors do store (linhas 67-79), estado local e derived values (linhas 80-116), handlers (linhas 120-129), e o ponto de inserção no JSX entre o fieldset de carga (~linha 365) e a div de Configurações Avançadas (~linha 367)
    - src/components/WelcomeOverlay.tsx — linhas 251-283: padrão exato de checkbox peer/sr-only/SVG para replicar fielmente
  </read_first>
  <action>
**Passo 1 — Adicionar `LayoutGrid` ao import de lucide-react (linhas 3-15):**

Incluir `LayoutGrid` na lista existente:
```typescript
import {
  Play, Globe, Users, Clock, ChevronDown, ChevronUp,
  AlertCircle, Loader2, Layers, Server, BookOpen, Save,
  LayoutGrid,  // ADICIONAR
} from "lucide-react";
```

**Passo 2 — Adicionar `MISTERT_MODULE_METADATA` ao import de test-presets (linhas 22-27):**

```typescript
import {
  MISTERT_OPERATION_COUNT,
  MISTERT_DEFAULT_BASE_URL,
  buildMistertOperations,
  MISTERT_MODULE_METADATA,  // ADICIONAR
} from "@/constants/test-presets";
```

**Passo 3 — Adicionar constante de módulo-level MISTERT_MODULE_NAMES após as constantes MISTERT_ENVIRONMENTS e antes do componente TestConfig:**

```typescript
/** Set de nomes de módulos MisterT para lookup O(1) na detecção e filtragem. */
const MISTERT_MODULE_NAMES = new Set(MISTERT_MODULE_METADATA.map((m) => m.name));
```

**Passo 4 — Adicionar selector `updateModuleSelection` dentro do componente, junto aos outros selectors do store (após a linha ~78):**

```typescript
const updateModuleSelection = useTestStore((s) => s.updateModuleSelection);
```

**Passo 5 — Adicionar valores derivados dentro do componente, após as declarações de estado local (após linha ~97, antes do primeiro useEffect):**

```typescript
// Detecção de preset MisterT e estado de seleção dos módulos
const isMistertPreset = (config.operations ?? []).some((op) =>
  MISTERT_MODULE_NAMES.has(op.name)
);
const selectedModuleNames = new Set(
  (config.operations ?? [])
    .map((op) => op.name)
    .filter((n) => MISTERT_MODULE_NAMES.has(n))
);
const allModulesSelected = selectedModuleNames.size === MISTERT_MODULE_METADATA.length;
const noModulesSelected = isMistertPreset && selectedModuleNames.size === 0;
```

**Passo 6 — Adicionar handlers após `handleEnvironmentChange` e antes de `handleStart`:**

```typescript
/**
 * Toggle de módulo individual: reconstrói operations[] do template,
 * mantendo infra ops [0-2] e filtrando módulos pela nova seleção.
 */
const handleModuleToggle = useCallback(
  (moduleName: string, checked: boolean) => {
    const allOps = buildMistertOperations(currentBaseUrl);
    const infraOps = allOps.slice(0, 3); // Página de Login, Login, Menu Principal (fixos)
    const newSelectedNames = new Set(
      checked
        ? [...selectedModuleNames, moduleName]
        : [...selectedModuleNames].filter((n) => n !== moduleName)
    );
    const moduleOps = allOps.slice(3).filter((op) => newSelectedNames.has(op.name));
    updateModuleSelection([...infraOps, ...moduleOps]);
  },
  [currentBaseUrl, selectedModuleNames, updateModuleSelection],
);

/** Seleciona todos os 7 módulos (restaura o template completo). */
const handleSelectAll = useCallback(() => {
  updateModuleSelection(buildMistertOperations(currentBaseUrl));
}, [currentBaseUrl, updateModuleSelection]);

/** Desmarca todos os módulos — mantém apenas as 3 infra ops fixas. */
const handleClearAll = useCallback(() => {
  const allOps = buildMistertOperations(currentBaseUrl);
  updateModuleSelection(allOps.slice(0, 3));
}, [currentBaseUrl, updateModuleSelection]);
```

**Passo 7 — Inserir seção JSX no render, APÓS o fieldset "Configuração de carga" (</fieldset> ~linha 365) e ANTES da div "Configurações Avançadas" (<div className="mb-4"> ~linha 367):**

```tsx
{/* ---- MÓDULOS DO TESTE ---- */}
{isMistertPreset && (
  <fieldset className="mb-4">
    <legend className={labelClass}>
      <LayoutGrid className="w-4 h-4" aria-hidden="true" />
      Módulos do Teste
      <InfoTooltip text="Selecione quais módulos do MisterT incluir neste teste. Módulos desmarcados serão removidos do fluxo. Login e Menu Principal são sempre executados." />
    </legend>

    {/* Linha de toggle Selecionar Todos / Limpar Seleção + contador */}
    <div className="flex items-center justify-between mb-2">
      <button
        type="button"
        onClick={allModulesSelected ? handleClearAll : handleSelectAll}
        className="text-xs font-medium text-sf-primary hover:text-sf-primaryHover transition-colors"
      >
        {allModulesSelected ? "Limpar Seleção" : "Selecionar Todos"}
      </button>
      <span className="text-xs text-sf-textMuted">
        {selectedModuleNames.size} de 7 selecionados
      </span>
    </div>

    {/* Grid de checkboxes — 3 colunas, padrão peer/sr-only de WelcomeOverlay */}
    <div className="grid grid-cols-3 gap-x-4 gap-y-2">
      {MISTERT_MODULE_METADATA.map((module) => {
        const isChecked = selectedModuleNames.has(module.name);
        return (
          <label
            key={module.name}
            className="flex items-center gap-2.5 cursor-pointer group select-none"
          >
            <div className="relative flex items-center justify-center shrink-0">
              <input
                type="checkbox"
                checked={isChecked}
                onChange={(e) => handleModuleToggle(module.name, e.target.checked)}
                className="peer sr-only"
                aria-label={module.name}
              />
              {/* Caixa visual — padrão WelcomeOverlay linhas 260-278 */}
              <div className="w-[18px] h-[18px] rounded-md border border-sf-border bg-sf-surface peer-checked:bg-sf-primary peer-checked:border-sf-primary peer-focus-visible:ring-2 peer-focus-visible:ring-sf-primary/50 peer-focus-visible:ring-offset-1 peer-focus-visible:ring-offset-sf-bg transition-all duration-200">
                {isChecked && (
                  <svg
                    className="w-full h-full text-white p-0.5"
                    viewBox="0 0 12 12"
                    fill="none"
                    aria-hidden="true"
                  >
                    <path
                      d="M2.5 6L5 8.5L9.5 3.5"
                      stroke="currentColor"
                      strokeWidth="1.75"
                      strokeLinecap="round"
                      strokeLinejoin="round"
                    />
                  </svg>
                )}
              </div>
            </div>
            <span className="text-sm text-sf-text truncate">{module.name}</span>
            <span className="text-xs text-sf-textMuted font-mono px-1.5 py-0.5 bg-sf-surface rounded ml-auto shrink-0">
              {module.code}
            </span>
          </label>
        );
      })}
    </div>

    {/* Aviso quando nenhum módulo está selecionado */}
    {noModulesSelected && (
      <div role="status" className="mt-2 px-3 py-2 bg-sf-warning/10 rounded-lg animate-fade-in">
        <p className="text-xs text-sf-warning leading-relaxed">
          Nenhum módulo selecionado — o teste executará apenas login e menu.
        </p>
      </div>
    )}
  </fieldset>
)}
```

RESTRICOES:
- Não remover nem alterar nenhuma seção existente do componente (toolbar de presets, ambiente MisterT, Ver Operações, botão Executar)
- Usar `useCallback` em todos os 3 handlers (padrão existente do componente)
- Os 3 handlers devem incluir suas dependências corretas no array de dependências
- A seção JSX fica ENTRE </fieldset> da carga e <div className="mb-4"> de Configurações Avançadas
  </action>
  <verify>
    <automated>cd /c/Users/rafael.damasio/Desktop/Project/CPX-MisterT\ Stress && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/TestConfig.tsx importa `LayoutGrid` de "lucide-react"
    - src/components/TestConfig.tsx importa `MISTERT_MODULE_METADATA` de "@/constants/test-presets"
    - src/components/TestConfig.tsx contém `const updateModuleSelection = useTestStore((s) => s.updateModuleSelection)`
    - src/components/TestConfig.tsx contém `const MISTERT_MODULE_NAMES = new Set` (constante module-level)
    - src/components/TestConfig.tsx contém `const isMistertPreset`
    - src/components/TestConfig.tsx contém `handleModuleToggle` declarado com `useCallback`
    - src/components/TestConfig.tsx contém `handleSelectAll` declarado com `useCallback`
    - src/components/TestConfig.tsx contém `handleClearAll` declarado com `useCallback`
    - src/components/TestConfig.tsx contém `MISTERT_MODULE_METADATA.map` no JSX
    - src/components/TestConfig.tsx contém `role="status"` para o aviso de seleção vazia
    - `npm run build` retorna exit code 0
  </acceptance_criteria>
  <done>Seletor de módulos renderizado em TestConfig.tsx. Build TypeScript sem erros.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação humana do seletor de módulos</name>
  <files>none — nenhum arquivo modificado nesta task</files>
  <action>Verificar manualmente o comportamento do seletor de módulos conforme os passos listados em how-to-verify. Executar npm run dev e testar cada cenário.</action>
  <verify>
    <automated>MISSING — verificação humana obrigatória (comportamento visual e interativo)</automated>
  </verify>
  <done>Todos os 9 passos de verificação passaram conforme esperado. Usuário confirmou com aprovado.</done>
  <what-built>
    Seção "Módulos do Teste" adicionada ao formulário de configuração. Aparece apenas quando o
    preset MisterT está ativo. Exibe 7 checkboxes em grid 3 colunas com nome e código do módulo.
    Toggle "Selecionar Todos/Limpar Seleção" e contador. Aviso informativo quando 0 módulos marcados.
    Checkboxes manipulam config.operations sem zerar o preset ativo.
  </what-built>
  <how-to-verify>
    Execute `npm run dev` e abra a aplicação. Siga os passos:

    1. Sem carregar preset — estado inicial (app usa buildMistertOperations por padrão):
       ESPERADO: Seção "Módulos do Teste" visível com 7 checkboxes marcados e contador "7 de 7 selecionados"

    2. Clicar "Presets" → aplicar "MisterT Completo":
       ESPERADO: Seção permanece visível, todos 7 marcados, nome "MisterT Completo" aparece na toolbar

    3. Desmarcar "Estoque":
       ESPERADO: Checkbox Estoque desmarca. Contador mostra "6 de 7 selecionados". Nome "MisterT Completo" AINDA visível na toolbar (preset ativo não zerado)

    4. Desmarcar "Financeiro":
       ESPERADO: Contador mostra "5 de 7 selecionados". Toggle exibe "Selecionar Todos"

    5. Clicar "Limpar Seleção":
       ESPERADO: Todos 7 desmarcados. Aviso laranja "Nenhum módulo selecionado — o teste executará apenas login e menu." aparece abaixo do grid

    6. Clicar "Selecionar Todos":
       ESPERADO: Todos 7 remarcados. Aviso some. Toggle exibe "Limpar Seleção"

    7. Se existir um preset de usuário customizado (não-MisterT): aplicá-lo
       ESPERADO: Seção "Módulos do Teste" NÃO aparece (preset sem ops MisterT)

    8. Verificação visual dos checkboxes:
       ESPERADO: Checkbox marcado = fundo indigo (sf-primary). Desmarcado = borda sf-border.
       Checkmark SVG branco visível quando marcado. Código (ex: "R=89") alinhado à direita

    9. Verificação de acessibilidade básica:
       ESPERADO: Tab navega entre checkboxes. Espaço togla o checkbox focado
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → IPC | config.operations enviada ao engine via window.stressflow.test.start(). Boundary preexistente — phase 4 não adiciona novos canais IPC. |
| user interaction → config state | Checkboxes manipulam config.operations via updateModuleSelection. O input é puramente boolean (sem campos de texto). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | handleModuleToggle | mitigate | Operations reconstruídas exclusivamente de `buildMistertOperations()` (template compilado em src/). O handler filtra por nomes de MISTERT_MODULE_NAMES estáticos — sem injection surface: nenhum valor vem de input de texto do usuário. |
| T-04-05 | Denial of Service | zero-module state | accept | Usuário pode desmarcar todos os módulos e rodar teste apenas com infra ops. Comportamento válido e documentado em CONTEXT.md D3. Engine recebe config válida (3 ops). Aviso visual informativo exibido — teste não é bloqueado. |
| T-04-06 | Information Disclosure | selectedModuleNames | accept | Estado de seleção é local ao renderer (Zustand). Contém apenas nomes de funcionalidades do ERP (não PII, não credenciais). Não trafega para fora do processo renderer. |
</threat_model>

<verification>
- `npm run build` passa (TypeScript compilado sem erros)
- Seção "Módulos do Teste" aparece quando preset MisterT é aplicado
- 7 checkboxes renderizados (grid 3 colunas)
- Desmarcar módulo individual remove apenas aquela op de config.operations
- activePreset não zerado ao fazer toggle (nome do preset permanece visível na toolbar)
- "Selecionar Todos" / "Limpar Seleção" funciona corretamente
- Aviso aparece com 0 módulos selecionados
- Seção não aparece para presets sem ops MisterT
</verification>

<success_criteria>
- Usuário consegue selecionar subset de módulos MisterT via checkboxes sem editar JSON (PRESET-03)
- Preset ativo mantido durante toggles de módulo (D4)
- Regressão zero: estado inicial tem todos 7 módulos marcados
- Verificação humana aprovada
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-module-selector/04-02-SUMMARY.md`
</output>
