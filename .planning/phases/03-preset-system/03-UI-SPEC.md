---
phase: 03
slug: preset-system
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-06
---

# Phase 03 — UI Design Contract: Preset System

> Visual and interaction contract for the Preset System phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom Tailwind design system) |
| Preset | not applicable |
| Component library | none (hand-rolled components following existing patterns) |
| Icon library | lucide-react 0.468 |
| Font | Inter (sans), JetBrains Mono (mono) |

**Source:** Existing `tailwind.config.mjs` with `sf-*` token namespace. No shadcn detected. All new components must follow existing patterns from WelcomeOverlay (modal), Toast (feedback), CredentialAlert (inline alerts), and sf-card-interactive (card hover states).

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| xs | 4px | Icon-to-text gap inside buttons, badge internal padding |
| sm | 8px | Gap between card metadata items, gap between action icon buttons, button vertical padding, badge vertical padding, icon button padding |
| md | 16px | Card internal padding, modal body padding (px-4), grid gap between cards |
| lg | 24px | Modal section spacing (gap between header/grid/footer) |
| xl | 32px | Modal outer padding (px-8 pt-10 pb-8 matches WelcomeOverlay) |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions: none

**Source:** Existing Tailwind default scale + custom spacing in `tailwind.config.mjs`. Values align with patterns in WelcomeOverlay (`px-8 pt-10 pb-8`) and TestConfig (`gap-3`, `mb-4`, `px-4 py-2.5`).

---

## Typography

| Role | Size | Weight | Line Height | Tailwind Classes |
|------|------|--------|-------------|------------------|
| Body | 14px | 400 (regular) | 1.5 | `text-sm text-sf-text` |
| Label | 14px | 600 (semibold) | 1.5 | `text-sm font-semibold text-sf-textSecondary` |
| Heading | 18px | 600 (semibold) | 1.28 | `text-lg font-semibold text-sf-text` |
| Small | 12px | 600 (semibold) | 1.33 | `text-xs font-semibold text-sf-textMuted` |

**Source:** Existing component patterns. TestConfig uses `text-sm` for body/labels, `text-lg font-semibold` for the title heading, `text-xs text-sf-textMuted` for help text. WelcomeOverlay uses `text-2xl font-bold` but that level is not needed in preset UI. Badge text uses existing `text-2xs` (10px) or `text-xs` (12px). Only two weights allowed: 400 (regular) for body text, 600 (semibold) for all emphasis including labels, headings, small metadata, and badges.

---

## Color

| Role | Token | Hex | Usage in this phase |
|------|-------|-----|---------------------|
| Dominant (60%) | `sf-bg` | #0f1117 | Modal backdrop area behind overlay, page background |
| Secondary (30%) | `sf-surface` | #1a1d27 | Modal panel background, preset card backgrounds, input fields |
| Accent (10%) | `sf-primary` | #6366f1 | "Carregar Preset" CTA on cards, "Salvar Preset" CTA in dialogs, active state ring on loaded preset card, focus rings, "Presets" button hover tint |
| Destructive | `sf-danger` | #ef4444 | Delete icon button on user cards, delete confirmation CTA background |

Accent (`sf-primary` #6366f1) reserved for:
1. "Carregar Preset" button on each preset card (primary action)
2. "Salvar Preset" / "Atualizar" CTA in save dialog
3. Active/loaded preset card left border or ring indicator
4. Focus-visible ring on all interactive elements
5. Built-in badge background tint (`bg-sf-primary/10 text-sf-primary`)

Additional semantic colors used:
- `sf-accent` (#22d3ee): Built-in badge alternative accent, operation count metadata highlight
- `sf-warning` (#f59e0b): Duplicate name validation error inline message
- `sf-overlay` (rgba(0,0,0,0.6)): Modal backdrop (actual implementation uses `bg-black/70 backdrop-blur-sm` per WelcomeOverlay pattern)

**Source:** Existing `tailwind.config.mjs` color definitions. WelcomeOverlay uses `bg-black/70 backdrop-blur-sm` for backdrop. TestConfig CTA uses `bg-sf-accent` for the execute button. Modal CTA should use `bg-sf-primary` per WelcomeOverlay's "Comecar" button pattern.

---

## Component Inventory

### New Components

| Component | File | Purpose |
|-----------|------|---------|
| `PresetModal` | `src/components/PresetModal.tsx` | Full-screen overlay modal displaying preset grid. Entry point from TestConfig. |
| `PresetCard` | Inline in PresetModal or extracted | Individual preset card with metadata and action buttons. Two variants: built-in and user-created. |
| `SavePresetDialog` | `src/components/SavePresetDialog.tsx` | Small modal/dialog for naming a new preset or choosing update vs save-as. |

### Modified Components

| Component | Change |
|-----------|--------|
| `TestConfig.tsx` | Add "Presets" button and "Salvar Preset" button in a toolbar row above the environment selector. Add state for modal open/close. Import PresetModal and SavePresetDialog. |
| `test-store.ts` | Add `activePreset` state (name + id + isBuiltin or null), `presets` array, and actions: `loadPresets`, `setActivePreset`, `clearActivePreset`. |
| `src/types/index.ts` | Add `TestPreset` interface, `PresetSummary` type, and IPC channel types for presets namespace on `Window.stressflow`. |

### Reused Components

| Component | How reused |
|-----------|------------|
| `Toast` (via `useToast`) | Success/error feedback for all CRUD operations |
| `InfoTooltip` | Tooltip on "Presets" button explaining what presets are |
| Existing modal animation pattern from WelcomeOverlay | Replicate overlay-fade-in/out, modal-scale-in/out animations |

---

## Interaction Contract

### I1: Open Preset Modal

| Property | Value |
|----------|-------|
| Trigger | Click "Presets" button in TestConfig toolbar |
| Animation | Backdrop: `overlay-fade-in` (0.35s out-expo). Panel: `modal-scale-in` (0.4s out-expo). Reuse WelcomeOverlay keyframes. |
| Focus | On open, focus moves to the first preset card or close button |
| Z-index | `z-[9999]` (matches WelcomeOverlay) |

### I2: Close Preset Modal

| Property | Value |
|----------|-------|
| Triggers | (a) Click backdrop overlay, (b) Click X close button, (c) Press Escape key |
| Animation | Backdrop: `overlay-fade-out` (0.3s out-expo). Panel: `modal-scale-out` (0.3s out-expo). 300ms delay before unmount. |
| Focus | Returns to the "Presets" button that opened the modal |

### I3: Load Preset (Apply)

| Property | Value |
|----------|-------|
| Trigger | Click "Carregar Preset" button on any preset card |
| Behavior | (1) Close modal with animation. (2) Replace `config` in Zustand store with the preset's `config_json`. (3) If built-in, apply URL base replacement using current environment selector value. (4) Set `activePreset` in store. (5) Show toast success. |
| Toast | `toast.success("Preset '{name}' carregado")` |
| Loading state | Button shows `Loader2` spinner during IPC round-trip (debounce not needed; IPC is fast) |

### I4: Save Preset

| Property | Value |
|----------|-------|
| Trigger | Click "Salvar Preset" button in TestConfig toolbar |
| Behavior -- no active preset or built-in active | Open SavePresetDialog with empty name field, CTA "Salvar Preset" |
| Behavior -- user preset active | Open SavePresetDialog with two options: "Atualizar {name}" (overwrites) and "Salvar Como Novo" (prompts name) |
| Validation | Name is required. Name must be unique (check against existing presets). If duplicate, show inline error below input: "Ja existe um preset com este nome". |
| Toast | `toast.success("Preset '{name}' salvo com sucesso")` |
| Error | `toast.error("Nao foi possivel salvar o preset")` |

### I5: Rename Preset

| Property | Value |
|----------|-------|
| Trigger | Click rename icon (Pencil icon from lucide-react) on a user-created preset card |
| Behavior | Small dialog/popover with current name pre-filled in input, CTA "Renomear Preset" |
| Validation | Same as save: required, unique name |
| Toast | `toast.success("Preset renomeado para '{name}'")` |
| Restriction | Not available on built-in preset cards |

### I6: Delete Preset

| Property | Value |
|----------|-------|
| Trigger | Click delete icon (Trash2 from lucide-react) on a user-created preset card |
| Confirmation | Inline confirmation replaces the card actions area: "Excluir este preset?" with two buttons -- "Confirmar Exclusao" (sf-danger) and "Manter Preset" (sf-textMuted). No separate modal. |
| Behavior | On confirm: (1) Delete via IPC. (2) Remove card with exit animation. (3) If deleted preset was active, clear `activePreset`. (4) Show toast. |
| Toast | `toast.success("Preset '{name}' excluido")` |
| Restriction | Not available on built-in preset cards |

---

## Layout Contract

### Modal Panel

**Focal point:** The preset card grid is the modal's focal point. On open, the first card in the grid receives visual prominence via its position at top-left and the staggered entry animation draws the eye there.

```
+----------------------------------------------------------+
|  [X]                                                      |  <- Close button top-right
|                                                           |
|  Presets de Teste                                    (h)  |  <- Heading (text-lg font-semibold)
|  Selecione um preset para carregar ou gerencie...    (p)  |  <- Subtext (text-sm text-sf-textSecondary)
|                                                           |
|  +------------------------+  +------------------------+  |
|  | [Built-in badge]       |  | {Preset Name}          |  |  <- Grid: 2 columns
|  | MisterT Completo       |  | 5 operacoes | 50 VUs   |  |
|  | 10 operacoes | -- VUs  |  | 120s                   |  |
|  | --s                    |  |                         |  |
|  |   [Carregar Preset]    |  |[Carregar Preset][E][X]  |  |  <- Built-in: only load
|  +------------------------+  +------------------------+  |     User: load + rename + delete
|                                                           |
|  +------------------------+  +------------------------+  |
|  | {Preset Name}          |  |                         |  |
|  | 3 operacoes | 100 VUs  |  |  (empty slot or more    |  |
|  | 300s                   |  |   preset cards)         |  |
|  |                         |  |                         |  |
|  |[Carregar Preset][E][X]  |  |                         |  |
|  +------------------------+  +------------------------+  |
|                                                           |
+----------------------------------------------------------+
```

- Modal max-width: `max-w-2xl` (672px) -- wider than WelcomeOverlay's `max-w-lg` to accommodate 2-column grid
- Modal border-radius: `rounded-2xl` (matches WelcomeOverlay)
- Grid: `grid grid-cols-2 gap-4` on large viewports; `grid-cols-1` on narrow windows (<640px)
- Modal max-height: `max-h-[80vh]` with `overflow-y-auto sf-scrollbar-thin` on the card grid area
- Top decorative gradient bar: same as WelcomeOverlay (`h-1 bg-gradient-to-r from-sf-primary via-sf-accent to-sf-primary`)

### Preset Card

```
+-------------------------------------------+
|  [Badge: Built-in]           (only if)    |  <- Top-right badge
|                                           |
|  {Preset Name}                            |  <- text-sm font-semibold text-sf-text
|                                           |
|  10 operacoes  |  100 VUs  |  120s        |  <- text-xs text-sf-textMuted, pipe-separated
|                                           |
|  [Carregar Preset] [Rename] [Delete]      |  <- Action row at bottom
+-------------------------------------------+
```

- Card uses `sf-card-interactive` utility class (existing)
- Card padding: `p-4` (16px)
- Built-in badge: `text-2xs font-semibold uppercase tracking-wider bg-sf-primary/10 text-sf-primary px-2 py-1 rounded-md`
- Active preset indicator: `ring-2 ring-sf-primary/40 border-sf-primary` on the currently loaded card
- "Carregar Preset" button: `bg-sf-primary hover:bg-sf-primaryHover text-white text-xs font-semibold px-3 py-2 rounded-lg`
- Rename/Delete icon buttons: `p-2 rounded-md text-sf-textMuted hover:text-sf-textSecondary hover:bg-sf-surfaceHover` (matches WelcomeOverlay close button pattern)

### TestConfig Toolbar (new row)

```
+--------------------------------------------------+
|  [Presets icon] Presets    [Save icon] Salvar...  |  <- Two buttons, flex row
+--------------------------------------------------+
|                                                    |
|  (existing TestConfig content below)               |
```

- Position: Above the "Ambiente MisterT" fieldset, below the hero section
- Layout: `flex items-center gap-3 mb-4`
- "Presets" button: `flex items-center gap-2 px-4 py-2 bg-sf-surface border border-sf-border rounded-xl text-sm text-sf-textSecondary hover:text-sf-text hover:border-sf-textMuted transition-all` with `BookOpen` icon from lucide-react
- "Salvar Preset" button: same style as "Presets" button with `Save` icon from lucide-react
- Both buttons use the existing `inputBaseClass`-adjacent styling (surface bg, border, rounded-xl)

### Save Dialog

- Reuses the same modal overlay pattern but smaller: `max-w-sm` (384px)
- Single input field for preset name using existing `inputBaseClass`
- When updating active preset: two CTAs side by side -- "Atualizar {name}" (primary) and "Salvar Como Novo" (secondary/outline)
- When new save: single CTA "Salvar Preset" (primary)
- Cancel via backdrop click, X button, or Escape (same as PresetModal)

---

## Empty State

| Property | Value |
|----------|-------|
| Condition | No user-created presets exist (only built-in visible) |
| Heading | Not applicable (built-in always present, so grid is never fully empty) |
| Body | Below the built-in card, a subtle hint: "Salve sua configuracao atual como preset usando o botao 'Salvar Preset' na tela de configuracao." |
| Style | `text-sm text-sf-textMuted text-center py-8` in the grid area beside built-in |

---

## Copywriting Contract

All text in Brazilian Portuguese (pt-BR) per project constraint.

| Element | Copy |
|---------|------|
| Modal heading | Presets de Teste |
| Modal subheading | Selecione um preset para carregar ou gerencie suas configuracoes salvas. |
| "Presets" button label | Presets |
| "Save" button label (TestConfig) | Salvar Preset |
| Built-in badge | Built-in |
| Built-in preset name | MisterT Completo |
| Card "Load" CTA | Carregar Preset |
| Card metadata format | {N} operacoes \| {N} VUs \| {N}s |
| Save dialog heading (new) | Salvar Preset |
| Save dialog heading (update) | Atualizar Preset |
| Save dialog name label | Nome do preset |
| Save dialog name placeholder | Ex: MisterT - Apenas Estoque |
| Save dialog CTA (new) | Salvar Preset |
| Save dialog CTA (update) | Atualizar {name} |
| Save dialog CTA (save as) | Salvar Como Novo |
| Rename dialog heading | Renomear Preset |
| Rename dialog CTA | Renomear Preset |
| Delete inline confirm | Excluir este preset? |
| Delete confirm CTA | Confirmar Exclusao |
| Delete cancel CTA | Manter Preset |
| Validation: empty name | Informe um nome para o preset |
| Validation: duplicate name | Ja existe um preset com este nome |
| Toast: load success | Preset '{name}' carregado |
| Toast: save success | Preset '{name}' salvo com sucesso |
| Toast: update success | Preset '{name}' atualizado |
| Toast: rename success | Preset renomeado para '{name}' |
| Toast: delete success | Preset '{name}' excluido |
| Toast: save error | Nao foi possivel salvar o preset |
| Toast: load error | Nao foi possivel carregar os presets |
| Toast: delete error | Nao foi possivel excluir o preset |
| Toast: rename error | Nao foi possivel renomear o preset |
| Empty state hint | Salve sua configuracao atual como preset usando o botao 'Salvar Preset' na tela de configuracao. |
| InfoTooltip on "Presets" button | Presets sao configuracoes de teste prontas para reutilizar. O preset 'MisterT Completo' ja vem configurado com as 10 operacoes padrao do ERP. |

---

## Animation Contract

All animations reuse the WelcomeOverlay pattern. Keyframes are defined inline via `<style>` tag within PresetModal (same self-contained pattern as WelcomeOverlay).

| Animation | Keyframes | Duration | Easing |
|-----------|-----------|----------|--------|
| Modal backdrop in | `overlay-fade-in` (opacity 0 to 1) | 0.35s | `cubic-bezier(0.16, 1, 0.3, 1)` |
| Modal backdrop out | `overlay-fade-out` (opacity 1 to 0) | 0.3s | `cubic-bezier(0.16, 1, 0.3, 1)` |
| Modal panel in | `modal-scale-in` (scale 0.92 + translateY 16px to normal) | 0.4s | `cubic-bezier(0.16, 1, 0.3, 1)` |
| Modal panel out | `modal-scale-out` (normal to scale 0.92 + translateY 16px) | 0.3s | `cubic-bezier(0.16, 1, 0.3, 1)` |
| Card grid entry | `stagger-children` class on grid container | 0.4s per card, 50ms stagger | `cubic-bezier(0.16, 1, 0.3, 1)` |
| Card delete exit | `animate-fade-in` reversed (opacity + scale shrink) | 0.2s | ease-out |
| Save dialog in/out | Same as modal panel in/out | Same | Same |

---

## Accessibility Contract

| Requirement | Implementation |
|-------------|----------------|
| Modal role | `role="dialog" aria-modal="true"` on modal container |
| Modal label | `aria-labelledby` pointing to heading "Presets de Teste" |
| Focus trap | On open, focus moves to first focusable element (close button or first card). Tab cycles within modal. On close, focus returns to trigger button. |
| Escape dismiss | `keydown` listener for Escape key closes modal (reuse WelcomeOverlay pattern) |
| Button labels | All icon-only buttons have `aria-label`: rename = "Renomear preset", delete = "Excluir preset", close = "Fechar" |
| Card semantics | Each card is a `<div>` with role="group" and aria-label="{preset name}" |
| Active indicator | Active preset card: `aria-current="true"` |
| Delete confirmation | Inline confirmation uses `role="alert"` for screen reader announcement |
| Reduced motion | Existing `@media (prefers-reduced-motion: reduce)` in `index.css` disables all animations globally |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable (shadcn not initialized) |
| Third-party | none | not applicable |

**Note:** This project does not use shadcn or any component registry. All components are hand-rolled using Tailwind CSS with the `sf-*` design system. No registry vetting is required.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
