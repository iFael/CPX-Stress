---
phase: 04-module-selector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/test-presets.ts
  - src/stores/test-store.ts
autonomous: true
requirements:
  - PRESET-03
must_haves:
  truths:
    - "MISTERT_MODULE_METADATA exportado de src/constants/test-presets.ts com 7 entradas cujos nomes correspondem exatamente a MISTERT_OPERATIONS_TEMPLATE[3..9].name"
    - "updateModuleSelection em src/stores/test-store.ts atualiza config.operations e config.url sem zerar activePreset"
    - "npm run build passa sem erros TypeScript após as duas modificações"
  artifacts:
    - path: "src/constants/test-presets.ts"
      provides: "Constante MISTERT_MODULE_METADATA com 7 módulos de negócio"
      contains: "export const MISTERT_MODULE_METADATA"
    - path: "src/stores/test-store.ts"
      provides: "Ação updateModuleSelection na interface TestActions e na implementação do store"
      contains: "updateModuleSelection"
  key_links:
    - from: "MISTERT_MODULE_METADATA[].name"
      to: "MISTERT_OPERATIONS_TEMPLATE[3..9].name"
      via: "correspondência de string exata — usado pelo gate de visibilidade da UI"
      pattern: "Produção"
    - from: "updateModuleSelection implementation"
      to: "set() call sem activePreset: null"
      via: "distingue de updateConfig que sempre zera activePreset"
      pattern: "updateModuleSelection"
---

<objective>
Define a camada de dados para o seletor de módulos: exporta os metadados dos 7 módulos de negócio
do MisterT e adiciona a ação de store que atualiza as operações selecionadas sem zerar o preset ativo.

Purpose: Estabelece o contrato de dados que TestConfig.tsx consumirá no Plan 02 para renderizar checkboxes
e reconstruir config.operations ao togglear módulos.
Output: MISTERT_MODULE_METADATA exportado de test-presets.ts; updateModuleSelection implementado no store Zustand.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-module-selector/04-CONTEXT.md
</context>

<interfaces>
<!-- Contratos que o executor precisa conhecer antes de modificar os arquivos -->

From src/constants/test-presets.ts (exports atuais):
```typescript
export const MISTERT_DEFAULT_BASE_URL: string;  // "https://dev-mistert.compex.com.br"
export const MISTERT_OPERATION_COUNT: number;    // 10
export function buildMistertOperations(baseUrl?: string): TestOperation[];

// Template interno MISTERT_OPERATIONS_TEMPLATE (NÃO exportado) — 10 operações:
// [0] "Página de Login" — infra, fixo
// [1] "Login"           — infra, fixo
// [2] "Menu Principal"  — infra, fixo
// [3] "CPX-Fretes"      — módulo selecionável
// [4] "CPX-Rastreio"    — módulo selecionável
// [5] "Estoque"         — módulo selecionável
// [6] "Ordens E/S"      — módulo selecionável (barra no nome)
// [7] "Produção"        — módulo selecionável (ã com acento)
// [8] "Faturamento"     — módulo selecionável
// [9] "Financeiro"      — módulo selecionável
```

From src/stores/test-store.ts — interface TestActions atual (linhas 150-217):
```typescript
interface TestActions {
  setView: (view: AppView) => void;
  updateConfig: (partial: Partial<TestConfig>) => void;  // ZERA activePreset
  setStatus: (status: TestStatus) => void;
  setProgress: (data: ProgressData) => void;
  clearProgress: () => void;
  setCurrentResult: (result: TestResult | null) => void;
  setHistory: (history: TestResult[]) => void;
  addToHistory: (result: TestResult) => void;
  setError: (error: string | null) => void;
  setCredentialStatus: (status: CredentialStatus | null) => void;
  applyPreset: (config: TestConfig, presetInfo: ActivePresetInfo) => void;
  setPresets: (presets: TestPreset[]) => void;
  clearActivePreset: () => void;
  // updateModuleSelection: A SER ADICIONADO neste plano
}
```

From src/types/index.ts (TestOperation — type para o parâmetro da nova ação):
```typescript
export interface TestOperation {
  name: string;
  url: string;
  method: HttpMethod;
  headers?: Record<string, string>;
  body?: string;
  captureSession?: boolean;
  extract?: Record<string, string>;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Exportar MISTERT_MODULE_METADATA de test-presets.ts</name>
  <files>src/constants/test-presets.ts</files>
  <read_first>
    - src/constants/test-presets.ts — ver exports existentes, style do arquivo e localização de MISTERT_OPERATIONS_TEMPLATE antes de modificar
  </read_first>
  <action>
Adicionar o bloco abaixo APÓS a declaração de `MISTERT_OPERATIONS_TEMPLATE` (linha ~95, após o `];` de fechamento do array) e ANTES da linha `export const MISTERT_OPERATION_COUNT`:

```typescript
/**
 * Metadados dos 7 módulos de negócio selecionáveis do MisterT ERP.
 *
 * Usado pelo seletor de módulos na UI para mapear checkboxes para operações.
 * Os nomes DEVEM ser idênticos a MISTERT_OPERATIONS_TEMPLATE[3..9].name —
 * a UI usa correspondência exata de string para detectar módulos no config.operations.
 * A ordem reflete a ordem de execução no template.
 */
export const MISTERT_MODULE_METADATA = [
  { name: "CPX-Fretes",   code: "R=89"  },
  { name: "CPX-Rastreio", code: "R=90"  },
  { name: "Estoque",      code: "R=122" },
  { name: "Ordens E/S",   code: "R=102" },
  { name: "Produção",     code: "R=84"  },
  { name: "Faturamento",  code: "R=206" },
  { name: "Financeiro",   code: "R=250" },
] as const;
```

CRITICO — verificar antes de salvar:
- "Produção" usa ã (acento til) — idêntico a MISTERT_OPERATIONS_TEMPLATE[7].name
- "Ordens E/S" usa barra "/" — idêntico a MISTERT_OPERATIONS_TEMPLATE[6].name
- Não modificar nada mais no arquivo (buildMistertOperations e demais exports permanecem intactos)
  </action>
  <verify>
    <automated>cd /c/Users/rafael.damasio/Desktop/Project/CPX-MisterT\ Stress && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/constants/test-presets.ts contém a linha `export const MISTERT_MODULE_METADATA`
    - O array tem exatamente 7 entradas (7 objetos com chaves name e code)
    - A entrada com "Produção" usa ã: grep retorna "Produção" não "Producao"
    - A entrada "Ordens E/S" inclui a barra: grep retorna "Ordens E/S"
    - `npm run build` retorna exit code 0
  </acceptance_criteria>
  <done>MISTERT_MODULE_METADATA exportado com 7 módulos cujos nomes correspondem exatamente ao template.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar updateModuleSelection ao store Zustand</name>
  <files>src/stores/test-store.ts</files>
  <read_first>
    - src/stores/test-store.ts — ver linha de imports (44-59) para adicionar TestOperation, interface TestActions (150-217) para posicionar nova assinatura, e bloco de ações de preset (~380-388) para posicionar a implementação
  </read_first>
  <action>
**Passo 1 — Adicionar `TestOperation` ao bloco de imports de "@/types" (linha ~44-55):**

```typescript
import type {
  TestConfig,
  TestResult,
  ProgressData,
  AppView,
  TestStatus,
  SecondMetrics,
  CredentialStatus,
  TestPreset,
  ActivePresetInfo,
  TestOperation,    // ADICIONAR
} from "@/types";
```

**Passo 2 — Adicionar assinatura à interface `TestActions`, após `clearActivePreset` (linha ~216):**

```typescript
  /**
   * Atualiza as operações do teste sem limpar o preset ativo.
   * Usado pelo seletor de módulos para personalizar o fluxo MisterT
   * sem perder a referência ao preset carregado (D4).
   *
   * Atualiza config.operations e config.url (primeira operação).
   * NÃO zera activePreset — diferente de updateConfig que sempre zera.
   */
  updateModuleSelection: (operations: TestOperation[]) => void;
```

**Passo 3 — Implementar a ação em `useTestStore = create<TestStore>((set) => ({ ... }))`:**

Adicionar após `clearActivePreset: () => set({ activePreset: null }),` (linha ~388):

```typescript
  updateModuleSelection: (operations) =>
    set((state) => ({
      config: {
        ...state.config,
        operations,
        url: operations[0]?.url ?? state.config.url,
      },
      // activePreset NÃO é zerado — seleção de módulo é personalização temporária
      // do preset ativo para um teste específico (D4 de 04-CONTEXT.md)
    })),
```

PROIBIDO: Não usar `updateConfig` como base (ele zera activePreset). Não incluir `activePreset: null` no objeto passado ao `set()`.
  </action>
  <verify>
    <automated>cd /c/Users/rafael.damasio/Desktop/Project/CPX-MisterT\ Stress && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/test-store.ts importa `TestOperation` de "@/types"
    - src/stores/test-store.ts contém `updateModuleSelection: (operations: TestOperation[]) => void` na interface TestActions
    - A implementação de updateModuleSelection usa `set((state) => ({ config: { ...state.config, operations, url: ... } }))` sem incluir `activePreset: null`
    - `npm run build` retorna exit code 0
  </acceptance_criteria>
  <done>updateModuleSelection implementada: atualiza config.operations e config.url sem zerar activePreset. TypeScript compila sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | config.operations é passada ao engine via window.stressflow.test.start(). Boundary preexistente — phase 4 não adiciona novos canais IPC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | MISTERT_MODULE_METADATA | mitigate | Array declarado `as const` — imutável em runtime. Nenhuma entrada de usuário pode alterar os metadados. |
| T-04-02 | Tampering | updateModuleSelection | mitigate | O parâmetro `operations` vem de `buildMistertOperations()` (template compilado), não de input direto do usuário. A UI filtra por nome contra metadados estáticos — sem superficie de injeção. |
| T-04-03 | Denial of Service | config.operations vazia | accept | Usuário pode deselecionar todos os módulos e executar teste só com infra ops. Comportamento válido documentado em CONTEXT.md D3. UI mostrará aviso informativo (implementado no Plan 02). |
</threat_model>

<verification>
- `npm run build` passa após ambas as modificações
- `grep "export const MISTERT_MODULE_METADATA" src/constants/test-presets.ts` retorna 1 linha
- `grep "updateModuleSelection" src/stores/test-store.ts` retorna pelo menos 2 linhas (interface + implementação)
- `grep "activePreset: null" src/stores/test-store.ts` na implementação de updateModuleSelection NÃO retorna resultado
</verification>

<success_criteria>
- MISTERT_MODULE_METADATA com 7 entradas exportado de test-presets.ts, nomes idênticos ao template
- updateModuleSelection implementada no store sem zerar activePreset
- Build TypeScript sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-module-selector/04-01-SUMMARY.md`
</output>
